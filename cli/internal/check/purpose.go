package check

// TODO type PurposeResult struct

func Purpose() (matches bool, reason string, err error) {
	// Need: system, documentationID, document, section, purpose, currentDB

	// Prompt
	// Document/Section Contents
	// Purpose
	// Instructions

	// Tools
	// list_files
	// get_file(name)
	// list_documents
	// get_document(name)
	// list_sections(document)
	// get_section(document, ID)
	// purpose_match(reason)
	// purpose_mismatch(reason)

	// Call LLM

	return false, "TODO", nil
}
